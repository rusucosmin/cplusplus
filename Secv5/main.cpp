#include <algorithm>
#include <fstream>
using namespace std;
ifstream cin("secv5.in");
ofstream cout("secv5.out");
int n, l, u, a[100005], i, j;
int main()
{
    cin>>n>>l>>u;
    for(i=1;i<=n;i++)
        { cin>>a[i];
          s[i]=a[i];}
    sort(s+1, s+n+1);
    for(i=1;i<=)
    return 0;
}
