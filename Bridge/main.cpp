#include <fstream>
using namespace std;

ifstream cin("bridge.in");
ofstream cout("bridge.out");

short a[4005], k, unde[4001];
int m[4005][4005];

int main()
{
    cin>>n>>m;
    for(i=1;i<=n;i++)
        {
            cin>>a[i];
            if(a[i]==3])
                k++;
        }
    for(i=1;i<=k;i++)
        cin>>unde[i];
    for(i=1;i<=n;i++)
        for()

    cin.close();
    cout.close();
    return 0;
}
