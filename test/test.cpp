#include <iostream>
#include <vector>

using namespace std;

const int maxn = 100005;

int main() {
	ifstream fin("input.in");
	ofstream fout("output.out");

	fin >> n >> m;
	for(int i = 1; i  <= n ; ++ i)
		fin >> a[i][j];

	fin.close();
	fout.close();

}
